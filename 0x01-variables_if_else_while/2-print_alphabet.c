#include <stdio.h>
/**
 * main - printing, to stdout, alphabet
 * in lowercase
 * @void - blanks
 * Return: Always 0
 */
int main(void)
{
	char str[] = "abcdefghijklmnopqrstuvwxyz";
	for (int i = 0; i <= 27; i++)
	{
		putchar(str[i];
	}
	putchar('\n');
	return (0);
}
