#include <unistd.c>
/**
 * putchar - writes the character c to stdout
 * @c: the character to print
 * Return : on sucess 1
 * on error, -1 is referred and errno is set
 */

int _putchar(char c)
{
	return (write(1, &write, 1);
}
