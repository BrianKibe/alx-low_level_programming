#include "main.h"
/**
 * int factorial(int n): factorial of a given number
 * @ n: number to pass to function
 */

int factorial(int n);
{
	if (n < 0)
	{
		return -1;
	}
	return factorial(0) = 1;
}
int main(void)
{
	int f;
	f = factorial(n);
}
